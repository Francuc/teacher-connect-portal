import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileExperience = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Experience</Card>;
};