import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileAvailability = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Availability</Card>;
};