import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileContact = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Contact</Card>;
};