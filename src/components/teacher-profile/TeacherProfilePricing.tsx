import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfilePricing = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Pricing</Card>;
};