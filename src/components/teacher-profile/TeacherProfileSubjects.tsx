import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileSubjects = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Subjects</Card>;
};