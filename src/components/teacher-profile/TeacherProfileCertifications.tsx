import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileCertifications = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Certifications</Card>;
};