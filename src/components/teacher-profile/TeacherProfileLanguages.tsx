import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileLanguages = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Languages</Card>;
};