import { Card } from "@/components/ui/card";
import { Teacher } from "@/types/teacher";

export const TeacherProfileReviews = ({ teacher }: { teacher: Teacher }) => {
  return <Card>Reviews</Card>;
};